{{ if .Values.global.postgresSecret }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.postgresConfigMap.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.postgresConfigMap.labels.app }}
data:
  POSTGRES_DB: {{ .Values.global.SSP_POSTGRES_DB | quote }}
  POSTGRES_USER: {{ .Values.global.SSP_POSTGRES_USER | quote }}
  POSTGRES_PASSWORD: {{ .Values.global.SSP_POSTGRES_PASSWORD | quote }}
{{ end }}  

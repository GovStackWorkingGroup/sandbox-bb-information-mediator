{{ if .Values.global.postgresSecret }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.postgresConfigMap.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.postgresConfigMap.labels.app }}
data:
  POSTGRES_DB: {{ .Values.global.CS_POSTGRES_DB }}
  POSTGRES_USER: {{ .Values.global.CS_POSTGRES_USER }}
  POSTGRES_PASSWORD: {{ .Values.global.CS_POSTGRES_PASSWORD }}
{{ end }}  
